# atcoder
road to 🩶🐈🤎🟢🩵🔵🟡🧡🔴

# template

```typescript
import * as fs from "fs";
// `A B C
//  i j k`
const input = fs.readFileSync("/dev/stdin", "utf8").split("\n");
const firstLine = input[0].split(" ");
const secondLine = input[1].split(" ");

const A = firstLine[0];
const B = firstLine[1];
const C = firstLine[2];

const i = secondLine[0];
const j = secondLine[1];
const k = secondLine[2];


function main(input: string): string {
  return input;
}

console.log(main(input))
```